export default function TokenScannerLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="min-h-screen w-full bg-[#050510] text-white">
      {children}
    </div>
  );
}
