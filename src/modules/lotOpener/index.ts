// MODULE — lotOpener (public entry point). See lotOpener.ts for the full disclosure on naming,
// cost-basis honesty, the "only BUY" vs. "LP_ADD" spec contradiction, and the LP_ADD
// "both tokens" data-availability limitation.

export { openLots, type IntentLot } from './lotOpener'
