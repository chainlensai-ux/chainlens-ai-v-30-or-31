// MODULE — lotCloser (public entry point). See lotCloser.ts for the full disclosure on naming,
// currency honesty (proceeds/costBasis mismatch handling), and the "no matching lot" behavior.

export { closeLots, type ClosedLot, type CloseLotsResult } from './lotCloser'
