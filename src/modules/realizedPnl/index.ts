// MODULE — realizedPnl (public entry point). See pnlSummary.ts for the full disclosure on naming
// (vs. the pre-existing src/modules/pnlEngine) and currency-honesty aggregation rules.

export { computeRealizedPnl, type RealizedPnlSummary } from './pnlSummary'
