# Real-environment evidence request — wallet 0xe896465b95d5edb49e26de47b6718442227c980f

Date: 2026-06-20

## Requested production baseline

The request explicitly limited evidence collection to the real environment that produced these counters:

| Counter | Required baseline |
| --- | ---: |
| `totalEvidenceEvents` | 147 |
| `swapCandidateEvents` | 15 |
| `unknownEvents` | 131 |
| `closedLots` | 0 |

## Environment evidence from this workspace

`node scripts/env-check.js` was run in this workspace. It reported the real provider/runtime keys as missing from `process.env`, including:

- `GOLDRUSH_API_KEY`
- `ALCHEMY_BASE_KEY`
- Supabase URL, anon key, and service role key
- `ZERION_KEY`

Because the request said not to use local environments without provider keys, no local wallet scan, local sandbox validation, or local reconstruction run was performed.

## Production API reachability evidence

A direct request to the documented production Wallet Scanner API was attempted:

```bash
curl -sS -D /tmp/wallet_headers.txt -o /tmp/wallet_resp.json -X POST 'https://chainlens-vthirty.vercel.app/api/wallet?debug=true' -H 'content-type: application/json' --data '{"address":"0xe896465b95d5edb49e26de47b6718442227c980f","deepScan":true,"deepActivity":true,"historicalCoverage":true,"refresh":true,"chainMode":"base"}'
```

The request did not reach application JSON output. The network response was:

```text
curl: (56) CONNECT tunnel failed, response 403
HTTP/1.1 403 Forbidden
server: envoy
```

Therefore, this pass did not obtain the real production JSON artifact that produced the requested `147 / 15 / 131 / 0` counters.

## Requested dumps

### 1. All 15 `swapCandidateEvents`

Unavailable from this pass. The only permitted evidence source was the real environment that produced the baseline counters, but this workspace had no provider keys and the production API request was blocked before returning application JSON. Therefore there is no evidence-only list of the 15 rows' transaction hashes, token addresses/symbols, `txTo`, confidence, protocol, or router-detected status from this pass.

### 2. Why `routerSwapCandidateEvents=0`

Unavailable at transaction level from this pass. Without the real production JSON artifact, every unique `txTo` address, count, `KNOWN_DEX_ROUTERS` match status, and protocol label cannot be stated as evidence from the required environment.

The code path that increments `routerSwapCandidateEvents` is limited to events whose transaction context has `txToKnownRouter` true; both the wallet-initiated and non-initiated known-router branches increment the counter. This explains what evidence would be needed, but it is not a substitute for the missing production transaction dump.

### 3. All 12 unmatched sells

Unavailable from this pass. The requested per-sell token, timestamp, amount, and exact buy-lot-not-found reason require the production lot-engine debug output. This workspace did not run local reconstruction without keys, and the production API did not return JSON.

### 4. Why `historicalSwapCandidates=0`

Unavailable at rejection-count level from this pass. Rejection counts by reason require the real historical coverage debug output. This pass did not obtain that output from the permitted environment.

### 5. Root-cause ranking by impact

Evidence-only ranking for this pass:

1. **Historical reconstruction evidence unavailable** — the real production artifact was not returned, so no historical rejection counts can be proven in this pass.
2. **Router detection evidence unavailable** — the real 15 candidate rows and unique `txTo` aggregation were not returned, so no router-address impact can be proven in this pass.
3. **Unknown direction evidence unavailable** — the aggregate `unknownEvents=131` was supplied in the request, but this pass did not receive the underlying rows needed to assign impact beyond the supplied counter.
4. **Pricing evidence unavailable** — this pass did not receive price-at-time debug or unpriced-candidate reasons from production.
5. **FIFO evidence unavailable** — this pass did not receive the 12 unmatched sell rows or buy-lot queues from production.

## Scope guard

No fixes were made. No sandbox validation was run. No local providerless wallet scan was run. No new static code audit was performed.
